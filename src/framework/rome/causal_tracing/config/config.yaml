model:
  type: "gpt2"
  name: "gpt2-medium"
  # type: "llama"
  # name: "microsoft/Phi-3-mini-4k-instruct"
  models_dir: "../../../models"
  device: "cuda"
  save_to_local: True

logging:
  log_level: "INFO"

generation:
  prompt: "The Eiffel Tower is in"
  max_new_tokens: 1